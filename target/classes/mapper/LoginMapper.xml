<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.forum.admin.mapper.LoginMapper">
    <select id="adminLogin" resultType="com.forum.admin.entity.AdminInfo" parameterType="java.lang.String">
        select * from tab_admin  where uphone=#{uname}
    </select>
</mapper>